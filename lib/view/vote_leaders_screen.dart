import 'package:flutter/material.dart';

class VoteLeadersScreen extends StatelessWidget {
  const VoteLeadersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('VoteLeaders Screen'),
    );
  }
}
